[project]
name = "fastapi-app-01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "graham", email = "u8360182@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "pydantic-settings>=2.14.1",
]

[project.scripts]
fastapi-app-01 = "fastapi_app_01:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
